import CartList from "@/components/cart/CartList";
import React from "react";

const Cart = ({ item }) => {
  return (
    <main className="container m-auto">
      <CartList />
    </main>
  );
};

export default Cart;
